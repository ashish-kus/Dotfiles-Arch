env=LIBSEAT_BACKENT,logind
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=/usr/lib/xfce-polkit/xfce-polkit
exec-once=~/ashishk/bin/sys/set-bg
exec-once=~/ashishk/bin/sys/idle
exec-once=~/ashishk/bin/sys/wbar.sh

exec-once=udiskie --config="~/.config/udiskie/config.yml" 
exec-once=wl-paste --watch cliphist store
#exec-once=/usr/lib/xfce-polkit/sway-audio-idle-inhibit
#exec-once=/usr/lib/kdeconnectd
#exec-once=kdeconnect-indicator
#exec=~/.config/hypr/init
#exec=~/.config/hypr/scripts/ui
#exec=~/.config/hypr/scripts/idle
#exec=~/.config/hypr/scripts/bar

#exec=~/.config/hypr/scripts/rule-clean
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################
#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,0x0,1
monitor= DP-1,preferred,1920x0,1
# monitor=  HDMI-A-2,preferred,3840x0,1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 2

    touchpad {
        natural_scroll = no
        disable_while_typing = true
        drag_lock = true
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

#------------------------------------ VARIABLES ------------------------------------------------#
general {
    gaps_in = 5                                 # gaps between windows.
    gaps_out = 10                               # gaps between windows and monitor edges.
    border_size = 3                             # size of the border around windows.
    col.active_border = rgba(7645ADaf)          # border color for the active window.
    col.inactive_border = rgba(7645AD5e)        # inactive (out of focus) group border color.
    cursor_inactive_timeout = 5                 # after how many seconds of cursor’s inactivity to hide it.
    layout = dwindle                            # window layout
    no_cursor_warps	= false                     # will not warp the cursor in many cases (focusing, keybinds, etc)
#   no_focus_fallback = false                   # will not fall back to the next available window when moving focus in a direction where no window was found
    resize_on_border = false                    # enables resizing windows by clicking and dragging on borders and gaps
    extend_border_grab_area= false              # extends the area around the border where you can click and drag on, only used when general:resize_on_border is on.
    # disable borders for floating windows.      
    no_border_on_floating = false                
}

blurls=rofi foot mako
blurls=waybar
blurls=rofi

#-------------------------- Decoration -------------------------------
# See https://wiki.hyprland.org/Configuring/Variables/ for more
decoration {
    # rounded corners’ radius (in layout px)
    rounding = 5
    active_opacity = 1.0                            # opacity of active windows
    inactive_opacity = 1.0                          # opacity of in-active windows
    fullscreen_opacity = 1.0                        # opacity of full screen window 
    blur = yes                                      # enable kawase window background blur
    blur_size = 2                                  # blue size
    blur_passes = 2                                 # the amount of passes to perform.
    blur_new_optimizations = on                     # whether to enable further optimizations to the blur.
    drop_shadow = yes
    shadow_range = 1
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

# ----------------------------   Gestures  -------------------------------
gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = yes
    workspace_swipe_fingers = 4
    workspace_swipe_create_new = true
}

# ---------------------------- MISC -------------------------------------
 misc {
    disable_hyprland_logo = true
 }

animations {
    enabled = yes
    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic mouse V1 {
    sensitivity = -0.5
}

# Example windowrule v1

windowrule = float, confirm
windowrule = float, splash
windowrule = float,^(telegramdesktop)$
windowrule = float,^(telegram-desktop)$
windowrule = float,^(org.telegram.desktop)$
windowrule = float, dialog
windowrule = float, wdisplay
windowrule = float, mpv
# windowrule = float,class:^(mpv)$
windowrule = float, "Authentication required"
windowrule = float,title:Volume Control
# windowrule = float, ^(kitty)$ 
# windowrule = float,class:^(org.telegram.desktop)$ 
# windowrule = float, class:^(pavucontrol)$ 
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# change by me
bind = $mainMod, ESCAPE, exec, wlogout -b 5 -p layer-shell -c 10px -r 10px -T 400px -B 400px
bind = $mainMod, v, exec,cliphist list | rofi -dmenu | cliphist decode | wl-copy
bind = ALT,SPACE, exec, rofi -show drun -display-drun "run" 
bind = $mainMod,SPACE, exec, ~/.config/rofi/rofi_scrip/launcher.sh 
bind = $mainMod, t, exec, foot
bind = $mainMod, b, exec, firefox
bind = $mainMod, w, exec, ~/ashishk/bin/sys/set-bg
bind= $mainMod,N,exec,networkmanager_dmenu
# bind =  $mainMod, n, exec,  networkmanager_dmene
bind = $mainMod, Print , exec, /usr/bin/grimblast --notify --cursor copy area
bind = $mainModSHIFT, Print , exec, /usr/bin/grimblast --notify --cursor save area



bind = $mainMod, Q, killactive, 
bind = $mainModSHIFT, Q, exit, 
bind = $mainMod, E, exec, nautilus
bind = $mainMod, f, togglefloating, 
bind = $mainMod, m, fullscreen,1
bind = $mainModSHIFT, f, fullscreen, 
bind = $mainMod, D, exec, bemenu-run 
bind = $mainMod, P, pseudo, # dwindle
bind = $mainModSHIFT, J, togglesplit, # dwindle
# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move move with mainMod + arrow keys
bind = $mainModSHIFT, h, movewindow, l
bind = $mainModSHIFT, l, movewindow, r
bind = $mainModSHIFT, k, movewindow, u
bind = $mainModSHIFT, j, movewindow, d



# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bind = ALT, Tab, exec, scratchpad -l -g 
bind = $mainMod, Tab, exec, scratchpad  

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
